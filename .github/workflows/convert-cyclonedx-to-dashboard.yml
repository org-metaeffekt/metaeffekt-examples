name: Convert CycloneDx To Dashboard

on:
  workflow_dispatch:
    inputs:
      cyclonedx:
        description: "The CycloneDX-Bom file location, relative to the processors directory."
        required: true
        default: ../keycloak-25.0.4/01-analyzed/keycloak-25.0.4-cyclone-dx-1.6_exporter.json


jobs:
  prepare_runner:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # Can not be a custom action.

      - name: Setup java environment
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'zulu'
          cache: 'maven'
          cache-dependency-path: './processors/processor-parent.xml'

      - name: Configure container
        uses: ./.github/actions/configure/container

      - name: Configure git
        uses: ./.github/actions/configure/git

  download_vulnerability_index:
    runs-on: ubuntu-latest
    steps:
      - name: Download vulnerability index
        uses: ./.github/actions/mirror/download-vulnerability-index

  convert_cyclonedx_to_inventory:
    runs-on: ubuntu-latest
    steps:
      - name: Convert cyclonedx to inventory
        uses: ./.github/actions/convert/cyclonedx-to-inventory
        with:
          cyclonedx-input: ${{ inputs.cyclonedx }}
          inventory-output: /tmp/converted.xls

  enrich_inventories:
    runs-on: ubuntu-latest
    steps:
      - name: Enrich inventories and create dashboard
        uses: ./.github/actions/advise/enrich-inventories
        with:
          input-inventory: ${{ env.converted-inventory }}
          input-database: ${{ env.index-directory }}
          output-inventory: /tmp/enriched.xls

  create_dashboard:
    runs-on: ubuntu-latest
    steps:
      - name: Create dashboard
        uses: ./.github/actions/advise/create-dashboard
        with:
          input-inventory: ${{ env.enriched-inventory }}
          input-database: ${{ env.index-directory }}
          output-dashboard: /tmp/dashboard.html

  # If using act, this step is expected to fail.
  post_run_actions:
    runs-on: ubuntu-latest
    steps:
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cylonedx-to-dashboard.html
          path: /tmp/dashboard.html
